name: "Custom Security Scanner"

on:
  workflow_run:
    workflows: ["build"]
    types:
      - completed

jobs:
  on-success:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Test Command
        run: ls
